import { PagePlaceholder } from "@/components/dashboard/page-placeholder"

export const metadata = {
  title: "Jobs",
}

export default function JobsPage() {
  return <PagePlaceholder title="Jobs" />
}
